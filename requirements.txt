# ML / training environment (run ml/train_knn.py, ml/features.py).
# The backend has its own pinned set in backend/requirements.txt.
numpy==1.24.3
pandas==1.5.3
scikit-learn==1.3.0
scipy==1.11.1
joblib==1.2.0
librosa==0.11.0
soundfile==0.12.1
praat-parselmouth==0.4.7
nolds==0.6.3
ctgan==0.12.1          # generative tabular augmentation (pulls in torch)
